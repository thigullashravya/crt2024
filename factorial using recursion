#include <stdio.h>
int factorial(int n)
{
    if(n==0)
    return 0;
    else
    return n%10+factorial(n/10);
   
}
int main() {
   int n;
   scanf("%d",&n);
   int k=factorial(n);
   printf("%d",k);
   // printf("Try programiz.pro");

    
}
1234
10

=== Code Execution Successful ===
